import React from 'react';

const LoadingPage = () => (
    <div className="loader">
        <img className="loader__image" src="/pictures/loader.gif" alt="Loading spinner" />
    </div>
);

export default LoadingPage;
